# Final Demo Claims Table

Use this table as the source of truth for presentation wording.

| Claim | Status | Evidence | Safe phrasing | Caveat |
|---|---|---|---|---|
| The final wet-run entrypoint is `scripts/autonomous_missed_pick_recovery.py`. | Supported | `autonomous_missed_pick_recovery_last_run.txt`; `data/run_snapshots/run_20260531_185839/manifest.json` | "The final wet run used the autonomous missed-pick recovery script." | README still lists older entrypoints. |
| The active placement sequence is `bag_local_aabb_joint`. | Supported | Latest run log; `config/place/place_config.py`; manifests | "The active planner sequence is `bag_local_aabb_joint`." | Older `volume_topdown_safe` fallback still exists. |
| The system uses YOLO segmentation for object masks. | Supported | `vision/yolo_segmenter.py`; latest run log | "YOLO provides the object masks and class labels." | Do not imply YOLO alone gives depth. |
| The final run used `yolo_weights/full_data.pt` with confidence 0.35. | Supported | Latest run log; `config/survey/survey_config.py` | "The final config loaded `full_data.pt` at 0.35 confidence." | We did not evaluate model accuracy here. |
| The survey uses a five-frame burst and requires 5/5 hits. | Supported | `config/survey/survey_config.py`; latest run log | "Candidates must persist across the full survey burst." | Miss watchdog uses different burst settings. |
| RAFT-Stereo is used for dense disparity. | Supported | `vision/raft_runner.py`; `vision/pick_survey_pipeline.py`; latest run log | "RAFT-Stereo turns the stereo pair into dense disparity." | It runs once per chosen survey frame, not once per every burst frame. |
| The point cloud is masked by the YOLO segment. | Supported | `vision/pick_candidate_builder.py`; `vision/pointcloud.py` | "The YOLO mask gates the disparity points used for geometry." | Bad masks can still affect geometry. |
| Candidates require at least 300 valid object points. | Supported | `config/survey/survey_config.py`; candidate builder | "The code rejects sparse point-cloud candidates." | This is a threshold, not a guarantee of perfect geometry. |
| Stereo depth and overhead XY are fused. | Supported | `vision/pick_xy_resolver.py`; survey config | "Stereo provides Z and size; overhead refines XY through H(z)." | Blend weight is 0.45 overhead, 0.55 stereo by default. |
| The active overhead calibration is height-indexed. | Supported | `robot_calibration_bundle.npz`; calibration report | "The overhead homography is stored at multiple Z layers and interpolated." | Active layers are 0, 50, 150, and 200 mm. |
| Calibration achieved about 8 mm stereo XYZ RMSE. | Supported | `robot_calibration_report.txt` | "The calibration report shows stereo robot XYZ RMSE of about 8 mm." | This is calibration-set RMSE, not a full runtime error bound. |
| Overhead homography RMS is about 4 to 12 mm depending on Z layer. | Supported | `robot_calibration_report.txt` | "The overhead H(z) layers are in the single-digit to low-double-digit mm RMS range." | The 50 mm layer is the worst reported at about 11.51 mm. |
| The pick phi is resolved from object geometry, using the active `centroid_shortest_ray_parallel` mode. | Supported | `config/pick/pick_config.py`; `vision/pick_phi_resolver.py`; latest run log | "The gripper yaw is chosen from the mask geometry." | It can fall back if overhead/mask geometry is unavailable. |
| Pick Z uses object surface plus gripper offset with safety clamps. | Supported | `motion/pick_z_policy.py`; `scripts/pick_one_place_one.py` | "Pick height is computed from estimated surface height, gripper offset, and shared Z safety rules." | Final config also enables a ground-model-plus-object-height override. |
| Local height-aware XY adjustment is active. | Supported | `config/pick/pick_config.py`; `scripts/pick_one_place_one.py` | "Before grasping, the code can bias XY toward a local top-region in the point cloud." | It falls back to default target XY on errors. |
| Dynamic grip is active. | Supported | `config/pick/servo_config.py`; `scripts/pick_one_place_one.py`; `Teensy_Code/Teensy_Code.ino` | "The final pick path uses current-aware dynamic grip." | It depends on INA219 availability and firmware response. |
| Dynamic height probing is active. | Not supported | `USE_DYNAMIC_PICK_HEIGHT=False` in `config/pick/servo_config.py`; pick helper skips DLR | "The firmware supports dynamic lower, but the final pick config skipped it." | Do not say DLR was used in the final path. |
| Dynamic release is active. | Not supported | `USE_DYNAMIC_RELEASE_FOR_PLACE=False` in `config/place/place_config.py` and latest run log | "The firmware supports dynamic release, but final placement used fixed servo release." | Dynamic release can be discussed as implemented, not active. |
| The planner is deterministic and 2.5D/3D AABB based. | Supported | `planning/bag_local_3d_aabb_planner.py` | "The planner searches deterministic AABB placements over floor and stack layers." | It is not a randomized optimizer or physics simulator. |
| The planner is globally optimal. | Not supported | Planner code returns first acceptable lower-layer or best scored fallback | "It is a deterministic constrained search with scoring." | Avoid "optimal." |
| The active planner checks support and overhang for stacked placements. | Supported | `planning/bag_local_3d_aabb_planner.py` | "Stacked placements need enough support and limited overhang." | It is geometric support, not full force/physics modeling. |
| The active selector is placement-aware. | Supported | `planning/autonomous_planning_sequences.py` | "The next pick is chosen based on whether it can fit in the current bag state." | The old largest-volume candidate filter still acts as filter/fallback context. |
| Floor placements are preferred before stacking in the active joint selector. | Supported | `_select_bag_local_joint()` in `planning/autonomous_planning_sequences.py` | "The selector fills floor placements before relying on stacks." | Among floor items it tends to prefer smaller footprint/volume first. |
| The wet-run planner used class-specific weight and fragility from `config/grocery_spec.json`. | Not proven for wet-run path | Active `_compute_bag_local_target()` does not pass `object_properties` into `plan_bag_local_aabb_placement()` | "Class-property scoring exists in planner/demo code, but the active wet-run call appears to use default planner properties." | Capstone viewer scripts do load the grocery spec. |
| The active bag scene is `New Bag Test`. | Supported | `config/place/place_config.py`; `config/surface_zones.json`; latest run log | "The bag-local frame comes from the `New Bag Test` scene." | Bag must be recalibrated if physically moved. |
| The bag footprint in the active scene is 260 x 150 mm. | Supported | `config/surface_zones.json` | "The active bag footprint is 260 by 150 mm." | Planning height is separately configured as 250 mm. |
| The final run reached 11 placed boxes in current bag state. | Supported with caveat | Latest run log tail: `[FLOW] placed_boxes count = 11`; latest manifest `placed_boxes` count 11 | "The latest saved wet run reached a final bag-state count of 11 placed boxes." | Do not equate this with 20 unique manifest rows. |
| The latest manifest proves 20 unique successful placements. | Not supported | Latest manifest has 20 rows but repeats `object_i` and image names; final `placed_boxes` count is 11 | "The manifest contains repeated rows; the final bag-state count is 11." | Use this as a caveat, not a claim. |
| Miss detection runs before release. | Supported | `place_or_detect_miss()` in `scripts/autonomous_missed_pick_recovery.py` | "The robot goes to place hover, checks for a missed pick, then releases if clear." | It checks configured visual evidence, not a force sensor on the held object. |
| The missed-pick watchdog skips RAFT during normal checking. | Supported | `run_yolo_only_miss_check()` prints that RAFT/disparity/pointcloud are intentionally skipped | "The normal watchdog is YOLO-only; RAFT runs only after a confirmed miss." | This keeps the release gate faster but relies on YOLO matching. |
| Python checks Cartesian soft limits before motion. | Supported | `hardware/robot.py`; latest run log | "Python verifies soft limits and samples motor-step paths before sending moves." | Firmware does not know the Cartesian keep-out boxes. |
| The Teensy firmware executes synchronized four-axis moves. | Supported | `Teensy_Code/Teensy_Code.ino`; `hardware/robot.py` | "Python sends `MOVESYNC_T`; the Teensy synchronizes the axes to the requested move time." | Actual motion time can be longer if axis limits require it. |
| The stereo camera is one USB side-by-side device. | Supported | `hardware/cameras/stereo_apriltag_viewer.py`; README note | "The stereo module is one 1280x480 device split into two 640x480 images." | Do not describe it as two independent USB cameras. |
| The system is fully collision-proof. | Not supported | Soft limits are Python-side and scene-specific; planner uses geometry approximations | "The software has layered safety checks and conservative geometry filters." | Avoid absolute safety claims. |
